type Task = 
 record {
   completed: bool;
   id: nat;
   "text": text;
 };
service : {
  addTask: (text) -> (nat);
  getProgress: () -> (float64) query;
  getTasks: () -> (vec Task) query;
  toggleTask: (nat) -> (bool);
}
